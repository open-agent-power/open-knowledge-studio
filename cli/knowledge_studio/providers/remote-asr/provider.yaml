id: remote-asr
execution: external
label: ??????
description: ?? OpenAI Whisper API ? Groq Whisper ??????????????????? GPU ????

provides:
  speech.transcribe:
    maturity: experimental

requirements:
  env: [OPENAI_API_KEY]
  mcp: openai

cost:
  estimated_per_call: token-based (varies by audio length)

known_limits:
  - ?? API key
  - ???????????????
  - ??????
  - ??????????

last_validated: 2026-08-04

user_impact:
  install: "?? OpenAI API Key ? Groq API Key"
  disk: "?????????????"
  runtime: "?????????? API ???????"
  execution: "remote"
  privacy: "?????????? API ???"
  cost: "????????????? API ????OpenAI/Groq ????????"
  skip_effect: "????????????????"
