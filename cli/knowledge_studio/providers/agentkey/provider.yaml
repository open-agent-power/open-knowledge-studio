id: agentkey
execution: external
label: AgentKey
description: ????????????? TikHub ??????????????Bilibili ????? API?

provides:
  web.fetch:
    maturity: validated
  web.extract:
    maturity: validated
  metadata.fetch:
    maturity: validated
  subtitle.fetch:
    maturity: experimental

requirements:
  mcp: agentkey

cost:
  estimated_per_call: 0.2-2 credits

known_limits:
  - ??????? full ??
  - ????????????????partial?
  - Bilibili?????????????metadata_only?
  - ?? OAuth ??????session ??????

last_validated: 2026-08-06

user_impact:
  install: "???? AgentKey ????? API Key ? MCP"
  disk: "???????"
  runtime: "???????????? API ?????"
  execution: "remote"
  privacy: "??????? AgentKey ??"
  cost: "? credits ???????"
  skip_effect: "???????/??/B???????????"

maturity_by_action:
  web.extract.zhihu: validated
  web.extract.wechat: validated_partial
  metadata.fetch.bilibili: validated
  subtitle.fetch.bilibili: experimental
